const mongoose = require('mongoose');
const Cube = require('./Cube');
const Accessory = require('./Accessory');

const attacheAccessorySchema = 
cubicle.accessories.findOne({})

module.exports = mongoose.model('AttachAccessory', attacheAccessorySchema);